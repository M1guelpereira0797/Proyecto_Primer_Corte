#include <stdlib.h>
#include<stdio.h>

    int numeros[5];
    char letras[10];
    float temperatura[7];



int main(){

    int numeros[5] = { 10, 20, 30, 40};
    char vocales[5] = {'a', 'e', 'i', 'o', 'u'};

    numeros[0]= 100;
    printf("%d", numeros[0]);

    return 0; 

}