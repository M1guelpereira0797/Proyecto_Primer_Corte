#include<stdio.h>

/**const int numKaprecar = 6174;
int main(){
    while(numero != numKaprecar);
        numero = VerificarLongitud(numero)[1]
        numero = int(DigitosCreciente(numeer))-int()
        numero = str(numero)
        contador +=1
    printf(f"*Constante de kaprecar alcanzada: {numero}");
    printf(f"");
    printf();
}**/


int main(){
    int numero = 2345;
    int contador = 0;
        while(numero!=1){
            numero = (numero%2==0) ? numero/2 : 3*numero+1;
            printf("%d\n", numero);
            contador++;
        }
        printf("Numero de veces %d\n", contador);
}